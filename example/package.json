{
  "scripts": {
    "build": "pumpkin-plugin-build gametest.ts build/gametest.wasm --abi 0.2",
    "build:v0.1": "pumpkin-plugin-build my-plugin.ts build/plugin-v0.1.wasm --abi 0.1"
  },
  "private": true,
  "devDependencies": {
    "@minecraft/server-gametest": "1.0.0-beta.1.21.60-preview.24",
    "@pumpkinmc/pumpkin-api-ts": "file:.."
  }
}
