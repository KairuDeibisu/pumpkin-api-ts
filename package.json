{
  "name": "@pumpkinmc/pumpkin-api-ts",
  "version": "0.1.1",
  "description": "Pumpkin plugin API for TypeScript",
  "license": "MIT OR Apache-2.0",
  "main": "dist/index.ts",
  "types": "dist/index.ts",
  "files": [
    "dist",
    "src/build.js",
    "wit"
  ],
  "publishConfig": {
    "tag": "dev"
  },
  "bin": {
    "pumpkin-plugin-build": "src/build.js"
  },
  "scripts": {
    "prepare": "jco guest-types wit/v0.1 -n plugin -o dist/bindings --name index"
  },
  "dependencies": {
    "componentize-qjs": "^0.4.5",
    "esbuild": "^0.28.2"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.34.0"
  }
}
